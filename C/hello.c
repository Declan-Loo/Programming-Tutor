// preprocessing -> compiling -> assembling -> linking
// inclluding header files containing function headers and their definitions (needed as part of the compilation process)
#include <stdio.h>

// int - the return type of the function 'main' - in this case it returns an integer. The function main is the entry point of the program. It does not take any arguments.
int main(){
    printf("Hello, World!\n");
    return 0;
}